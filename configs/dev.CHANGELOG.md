# Changelog

All notable changes to the `dev` environment configuration
(`configs/config.dev.yaml`) are documented here. Versions follow
[Semantic Versioning](https://semver.org) and are derived from
[Conventional Commits](https://www.conventionalcommits.org) scoped to
that file, tagged `configs-dev-v<version>`.
